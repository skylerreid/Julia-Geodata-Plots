This code takes a dataset of South American dams and extracts the data from Brazil. The locations and sizes of the dams are shown using a colorbar. 

Note that there are some weird indexing issues with indexing into the geometry field, so this may not be generalizeable to other shapefiles. 
